# 2026-07-05

## Frontend Code Quality Roadmap Implementation (AI-NOTE-0007)

全面执行了 AI-NOTE-0007 前端代码质量改进路线图（5 个阶段），修改 35+ 文件，TypeScript 零错误。

### 新增文件
- `hooks/useTimeout.ts`
- `types/css.d.ts`
- `components/chat/ToolBlockUI.css`
- `components/layout/SessionList.tsx`
- `components/ui/Toast.tsx`

### Phase 0: 关键 Bug
- EditFileWidget/ToolBlockUI: setState in render → useEffect
- NodeChatStream CSS 类名不匹配
- EdgeConfigPanel 数据映射持久化
- NodeInspectorDrawer cost_usd 空值崩溃
- 3 个 modal 的 useEffect 依赖修复

### Phase 1: React 反模式 & 性能
- 9 处 setTimeout cleanup
- ToolBlockUI inline <style> → CSS 文件
- SubagentCard 计时器修复, SubagentDetailPage useMemo
- WorkflowEditor/NodePropertiesPanel 防抖
- MarkdownContent isStreaming 死代码移除
- ReviewTab 事件监听器 ref 修复

### Phase 2: 类型安全
- any 消除 (FileTree, WorkflowCanvas, PermissionsTab, OverviewTab 等)
- confirm()/alert() → useConfirmDialog/console.error
- CSS 自引用修复 (8 处)
- @ts-ignore → CSSProperties augmentation

### Phase 3: 无障碍
- Sidebar: projects-header role="button" + keyboard
- OverviewTab/AgentTurn: toggle headers ARIA
- nav-item → <button>
- EdgeConfigPanel/NodePropertiesPanel: label htmlFor 关联

### Phase 4: 架构
- SessionList 组件提取 (Sidebar ~170 行减少)
- 命名常量提取 (WorkflowRunView)

### Phase 5: 错误处理
- WorkflowRunView isMounted 标志
- WorkflowEditor handleRun try/catch
- DocumentTab for-of 循环修复竞态
- ToastProvider 全局错误通知系统
- CacheMetrics unused import 移除

## Skill Script Execution System Implementation

实现了完整的 skill 脚本执行系统（7 个文件），cargo check 零错误。

### Architecture: Script-as-Tool
- 每个 skill 的 `scripts/` 目录中的脚本自动注册为独立工具 `skill.<name>.<script>`
- Per-turn 同步（refresh_context_segments 开头执行），自动 diff 注册/注销
- 通过 ProcessSupervisor 安全执行（进程组隔离 + 超时保护）

### 文件改动
1. `core/src/skills/manifest.rs` — +ScriptEntry struct, +scripts 字段, +嵌套 YAML parser, +3 tests
2. `core/src/skills/mod.rs` — +discover_scripts(), get_active_scripts(), scripts_context(), 更新 build_active_context/load_skill_context
3. `core/src/tools/script.rs` — NEW: SkillScriptTool (supervised + legacy paths, JSON→CLI arg conversion) +5 tests
4. `core/src/tools/mod.rs` — +pub mod script
5. `core/src/runtime/run/context.rs` — +sync_skill_scripts() per-turn, cache invalidation
6. `core/src/runtime/run/mod.rs` — +registered_script_tools tracking field
7. `core/src/permission/mod.rs` — skill.* prefix → System danger level

### Next Steps
- 需要解决 pre-existing test build 问题（PermissionPolicy 等在 test build 中未声明）才能运行新测试
- SKILL.md 格式说明 + 使用示例
